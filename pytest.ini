[pytest]
# Allow running tests from repo root without needing manual PYTHONPATH.
# (CI/Makefile already runs inside backend/, but this helps contributors.)
pythonpath = backend
testpaths = backend/tests
addopts = -q
